// Write a program to Print factors of a number. 
#include<stdio.h>
int main()
{
    int num , i, factor ;
    printf("Enter the number");
    scanf("%d",&num);
    for(i=2 ; i<=num ;i++)
    {
        while(num%i==0)
        {
            printf("%d  ",i);
            num = num/i;
        }
    }
    return 0;
}
